# Localized name of the language for display in language selection
language-name = English (Great Britain)

select-language = Select language:

save-button = Save

# Do not translate this key - it's only used for tests if fallback works
fallback-key = Fallback translation available
